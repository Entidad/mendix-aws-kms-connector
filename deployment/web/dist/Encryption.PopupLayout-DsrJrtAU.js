import { reactExports, asPluginWidgets, PlaceholderProperty } from './index-FQlVOlYf.js';
import { Placeholder } from './Placeholder-DFcDqEIU.js';

const React = { createElement: reactExports.createElement };
const { $Placeholder } = asPluginWidgets({ Placeholder });
const region$Main = [
  /* @__PURE__ */ React.createElement(
    $Placeholder,
    {
      key: "l.Encryption.PopupLayout.Main",
      $widgetId: "l.Encryption.PopupLayout.Main",
      content: PlaceholderProperty({
        "id": "Encryption.PopupLayout.Main"
      })
    }
  )
];
const content = {
  "Main": region$Main
};

export { content };
